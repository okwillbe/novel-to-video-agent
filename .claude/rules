name=测试执行规则
purpose:
  - 让 agent 在仓库根自动执行 run-tests.sh，并在失败时收集最小复现场景。

preconditions:
  - agent 运行目录为仓库根
  - .env（或 .env.example）位于仓库根

steps:
  1) verify-versions:
     - 运行: java -version, mvn -v, node -v, docker compose version
     - 如果任何工具缺失或版本不对 -> 打印 JSON {error:"version_mismatch", details:...} 并退出码 2
  2) start-deps:
     - 运行: docker compose -f docker-compose.yaml up mysql redis minio -d
     - 等待端口就绪，最多 120s (DB:13306, REDIS:6379, MINIO:9000)
     - 如果未就绪 -> 收集 docker ps, docker logs -> 写入 report/last-failure/ 并退出码 3
  3) init-db:
     - 运行 scripts/init-db.sh（输出记录到 logs/init-db.log）
     - 遇到临时错误时重试 SQL 初始化，最多 3 次
  4) run-tests:
     - 运行 scripts/run-tests.sh（输出记录到 logs/logs-run-tests.tar.gz）
     - 成功条件：脚本返回 0 且打印 "TESTS PASSED: all modules"
  5) on-failure:
     - 复制日志到 report/last-failure/
     - 生成 JSON 诊断报告，包含: failing_tests[], first_stack, suggested_fixes[]
  6) safe_modification:
     - agent 可以提议代码修复，但必须未经用户明确同意不得推送或创建 PR

timeouts:
  - 各步骤超时遵循 CLAUDE.md 默认值 (start-deps:120s, init-db:60s, run-tests:1200s)

outputs:
  - 成功时: stdout 包含 EXACT: TESTS PASSED: all modules
  - 失败时: report/last-failure/ 包含结构化诊断文件和摘要 JSON 打印到 stdout